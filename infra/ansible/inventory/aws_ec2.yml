---
plugin: aws_ec2
regions:
  - ap-south-1

filters:
  tag:Project: tricloud-vault
  tag:Environment: dev
  instance-state-name: running

keyed_groups:
  - key: tags.Environment
    prefix: env

compose:
  ansible_host: instance_id

hostnames:
  - instance-id
