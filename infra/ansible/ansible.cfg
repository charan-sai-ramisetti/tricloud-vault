[defaults]
inventory = ./inventory
host_key_checking = False
remote_tmp = /tmp/.ansible-${USER}/tmp
retry_files_enabled = False
timeout = 60

# SSM connection plugin settings
[ssh_connection]
pipelining = True

[inventory]
enable_plugins = aws_ec2, ini
